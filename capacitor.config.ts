import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.madadigital.net',
  appName: 'zamart',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
